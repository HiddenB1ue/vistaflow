/**
 * 自动生成的 API 类型
 *
 * 运行 `pnpm generate:types` 从后端 FastAPI OpenAPI spec 重新生成。
 * 生成的类型文件位于 `generated/api.d.ts`。
 *
 * 当前为占位文件 — 首次生成后此处将重导出生成的类型。
 */

// TODO: 首次运行 generate:types 后取消注释
// export type * from '../generated/api';
